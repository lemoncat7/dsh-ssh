# 1.7.0

正式版。适配 DSH 0.1.5，已在 0.1.5-rc.1 镜像部署验证。需要 Node.js ^22.19.0 或 >=24.0.0。

- 中英双语界面跟随宿主语言，切换时保留终端连接、目录、表单及编辑草稿；旧宿主缺少语言服务时回退中文。业务字段、用户内容及服务端权限逻辑不受翻译影响。
- 终端旁 SFTP 按实际宽度收起次要列，修复窄栏横向空白；文件下载与删除按钮在悬停/聚焦时显示，触屏保持可见。
- 路径栏链条图标控制跟随当前选中终端的目录。预览、路径编辑、上传及删除确认期间暂停跟随，不覆盖正在操作的内容。
- 新终端支持 Bash/Zsh/sh（含 dash/ash）会话级目录上报，不修改远端启动文件；其他 shell 需支持 OSC 7 或 CurrentDir。已有终端需要重连。
- 隐藏精确匹配的初始化脚本回显，保留欢迎信息、错误和正常输出。初始化完成后停止过滤；超时、异常和关闭时保留未匹配内容。

验证：141 项自动测试通过，含真实 Bash/dash PTY、分块回显、异常保留及目录上报测试；手机和桌面浏览器覆盖双语切换、窄栏、下载与目录跟随。
